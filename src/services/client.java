/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package services;

import utils.MyConnection;
import java.sql.Connection;
import java.sql.PreparedStatement;

/**
 *
 * @author MLLE-BAHRI
 * 
 */
/*
public class client {
    public void AjouterReservation(NewClaass){
Connection cnx2=MyConnection.getInstance().getCnx();
        
            String requete2 = "INSERT INTO salle (nom_artiste,num_salle,date) VALUES(?,?,?);";
            try {
           PreparedStatement pst =cnx2.prepareStatement(requete2);
            pst.setInt(1, S.getNum_salle());
            pst.setString(2, S.getType());
            pst.setString(3, S.getStatu());
            pst.setFloat(4, S.getPrix());
            pst.executeUpdate();
            System.out.println("Salle ajouté avec succés!!!");

        } catch (SQLException ex) {
            System.err.println(ex.getMessage());
        }
        
    }
    
    
}*/
